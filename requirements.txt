# Core dependencies
streamlit==1.36.0
PyYAML==6.0.1
python-dotenv==1.0.1

# Validation & Configuration
pydantic==2.6.0
pydantic-settings==2.1.0

# Database (for migrations - optional for now)
# alembic==1.13.1

# Data processing (likely already used by streamlit)
pandas>=2.0.0

# Session Management
extra-streamlit-components==0.1.71
